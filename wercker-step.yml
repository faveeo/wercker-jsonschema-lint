name: jsonschema-lint
version: 0.0.4
description: Validates a JSON file according to its schema, or a default schema.
    - lint
    - json
    - jsonschema
properties:
    include:
        type: string
        required: true
    schema:
        type: string
        required: false
        default: http://json-schema.org/schema